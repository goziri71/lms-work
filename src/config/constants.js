/**
 * Institution Constants
 * Display name and internal code for the institution
 */
export const INSTITUTION = {
  NAME: "WPU", // Display name shown to users
  CODE: "wpu", // Internal code used in database and API
  OLD_CODE: "wsp", // Legacy code for backward compatibility
};

export default INSTITUTION;

